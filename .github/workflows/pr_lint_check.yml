name: 'PR Lint Check'

on:
  pull_request_target:
    types: [opened, edited, synchronize, reopened]

permissions:
  pull-requests: write

jobs:
  lint-pr:
    runs-on: ubuntu-latest
    steps:
      - name: 'Validate PR Title'
        id: validate
        env:
          BRANCH_NAME: ${{ github.head_ref }}
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          title_is_valid=false

          title_regex="^(Release|Feature|Bugfix|Docs|Chore|Refactor|Style|Test|Post)\/(app|wear|web|repo)\/([a-z0-9-]+\/)?[a-z0-9-]+"
          if [[ "$PR_TITLE" =~ $title_regex || "$PR_TITLE" == 'New Crowdin updates' ]] then
            title_is_valid=true
          fi
          echo "PR title validation: $title_is_valid"
          
          echo "title_is_valid=$title_is_valid" >> $GITHUB_OUTPUT

      - name: 'Add PR Comment on Failure'
        # Run check fails
        if: steps.validate.outputs.title_is_valid == 'false'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const titleIsValid = ${{ steps.validate.outputs.title_is_valid }};
            const prTitle = "${{ github.event.pull_request.title }}";
            
            let errorMessages = [];

            if (!titleIsValid) {
              errorMessages.push(
                `### :x: Invalid PR Title\n\nThe PR title \`${prTitle}\` does not follow the required format: \`<type>/<scope>/<description>\`.\n- **Example:** \`Feature/web/add new contact form\``
              );
            }

            const commentBody = `
              ## PR Validation Failed
              ${errorMessages.join('\n\n---\n\n')}
              \nPlease correct the issues and push the changes to update this pull request.
              \nFor more info, please check the [Pull Request Process guidance](https://github.com/J-shw/Menstrudel?tab=contributing-ov-file#pull-request-process).
            `;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });

      - name: 'Mark Job as Failed'
        if: steps.validate.outputs.title_is_valid == 'false'
        run: exit 1
